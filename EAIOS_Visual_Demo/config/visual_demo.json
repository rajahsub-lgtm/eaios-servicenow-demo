{
  "notes": "The approval boundary, without a bound instance. This previously deep-linked into a personal ServiceNow developer instance; that URL and its record sys_ids are removed so the deployment carries nothing tied to one tenant. With instance_url empty, the panel presents the approval contract and the mapped payload rather than a link — which is the architecture, not an artefact of where it was first demonstrated. Set instance_url and populate records to re-enable deep links against any instance.",
  "instance_url": "",
  "assessment_table": "u_eaios_operational_assessment",
  "assessment_view": "eaios_demo",
  "records": {}
}
